/**
 * @file 示例@example
 */

/**
 * @func
 * @desc 计算两个数值的和
 * @param {number} a - 加数a
 * @param {number} b - 加数b
 * @returns {number} 返回a和b的和
 * @example
 * func10(1, 2);    // 返回3
 */
function func10(a, b) {
    return a + b;
}